package kotlinspecific

/**
*  kotlin can be run on iOS by Intel Multi-OS Engine
 * kotlin can be use to build desktop app by TornadoFX/javaFX
 * kotlin can be compiled to JS
 * kotlin is statically typed, oppose to Groovy/jRuby which are dynamically typed
 * kotlin is pragmatic, concise and safe
 *
 *
 *
 * kotlin build process
 * *.kt is compiled by kotlin compiler to *.class
 * *.class is compiled to *.jar
 * kt compiled code depends on the kotlin runtime library, which contains kotlin own standard library and extension that kotlin addes to the standard java api
 * 
 * */